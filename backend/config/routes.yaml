controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

refresh_token:
    path: /api/auth/refresh
    controller: gesdinet.jwtrefreshtoken.refresh_token_controller::refresh
    methods: ['POST']

